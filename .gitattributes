# Track data folder with Git LFS (excluding generated index files)
data/** filter=lfs diff=lfs merge=lfs -text
# Exclude generated index files from LFS (they're in .gitignore anyway)
data/**/*.index filter= -text
data/*.index filter= -text
